{
  "name": "mr-fez-menu-boards",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check dist/app.js && python3 scripts/check-menu.py",
    "start": "python3 -m http.server 4173 --directory dist"
  }
}
